---
"svsch": minor
---

Add `svsch.fileList` setting to compile a project from a Surelog filelist (`-f`) instead of walking `svsch.projectFolder`, for multi-directory projects, explicit compile order, or library-style resolution.
