---
"svsch": patch
---

Hide the always-visible diagnostics panel and surface warning presence as a status icon in the toolbar instead, mutually exclusive with the busy spinner. The icon shows only a warning count on hover, no message text or per-severity breakdown.
