---
"svsch": patch
---

Hide the always-visible diagnostics panel and surface warning presence as a status icon in the toolbar instead, mutually exclusive with the busy spinner. The icon shows the warning count inline (e.g. "⚠ 3 warnings"), no per-severity breakdown.
