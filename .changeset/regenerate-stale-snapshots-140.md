---
"svsch": patch
---

Regenerate snapshot baselines for `docs/syntax-book/assets/mux-*.svg` and `test/visual/__screenshots__/mux.visual.spec.ts-snapshots/*.svg` that went stale from merge skew between #108 and #115, so they pick up #108's `font-family` fix for interface type labels. No source changes.
