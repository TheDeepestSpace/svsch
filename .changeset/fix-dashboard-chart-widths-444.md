---
"svsch": patch
---

Constrain dashboard `img` and inline `svg` charts to the container width (with `height:auto`) so they scale to fill available space instead of overflowing on narrow viewports.
