---
"svsch": patch
---

Re-add the `test/system` video gallery (`publish_system_videos`, `comment_video_galleries`, and the `vscodeVideo`/JSON reporter config in `test/system/playwright.config.ts`) that had been silently dropped by a stale branch merge.
