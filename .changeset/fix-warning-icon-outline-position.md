---
"svsch": patch
---

Anchor the node warning icon to the outline's top-right vertex instead of the bounding box corner, so it no longer floats off shaped nodes (mux, select, ALU, inverter, and skinned port/interface-port nodes).
