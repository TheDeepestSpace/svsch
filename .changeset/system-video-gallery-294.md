---
"svsch": patch
---

Record per-scenario videos in the `test/system` suite (across the 3-way `vscode_version` matrix) and publish them to a gh-pages gallery, mirroring the BDD video gallery added in #275.
