---
"svsch": patch
---

Add ESLint (flat config) and Prettier for `src/`, `test/`, and `scripts/` TS/TSX/JS code, with a 100-character line limit enforced in both, wired into CI's Lint step. No runtime behavior changes.
