---
"svsch": patch
---

Fix `Cut out`: the cut instance and its newly-created cut-net-end stubs are now reselected after the operation, so the group can be dragged immediately instead of requiring a manual reselect.
